[project]
name = "src"
version = "0.0.1"
description = "Sentiment Anysis on OLIST Dataset"
authors = [{name = "Pranjal"}]

[tool.setuptools]
packages = {find = {}}

[tool.setuptools.dynamic]
dependencies = {file = "requirements.txt"}